[tool.poetry]
name = "fastapi-lab1"
version = "0.1.0"
description = "Lab 1 Template"
authors = ["Student"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "*"
uvicorn = {extras = ["standard"], version = "^0.44.0"}
psycopg2-binary = "^2.9.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"